---
description: Add XML docs (///) to a C# file via the dotnet-documenter agent
agent: builder
subtask: true
---

First, load and follow my repo skill:

- Call the `skill` tool to load: dotnet-best-practices

Then, Add missing C# XML documentation comments (`///`) to the code provided in $ARGUMENTS.
