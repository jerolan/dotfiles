---
description: Deduplicate .NET tests
agent: builder
subtask: true
---

First, load and follow my repo skill:

- Call the `skill` tool to load: dotnet-test-deduper

Then, Deduplicate .NET tests using the code provided in $ARGUMENTS.
